# Copy to .env only when overriding these defaults.
TRAFFIC_STATUS_PORT=18080
CACHE_TTL_SECONDS=600
UPSTREAM_TIMEOUT_SECONDS=15
LOG_LEVEL=INFO

# Optional: set this only when the HostVDS account contains multiple servers.
HOSTVDS_SERVER_ID=
